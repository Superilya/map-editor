export enum Users {
    SELF = '/api/users/self',
    LIST = '/api/users/list'
}

export enum Map {
    BUILDINGS = '/api/map/buildings',
    ROOMS = '/api/map/rooms',
    PLACES = '/api/map/places'
}

export enum Auth {
    UI = '/auth'
}
