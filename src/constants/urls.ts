export enum Users {
    SELF = '/api/users/self'
}

export enum Map {
    BUILDINGS = '/api/map/buildings',
    ROOMS = '/api/map/rooms',
    PLACES = '/api/map/places'
}

export enum Auth {
    UI = '/auth'
}
